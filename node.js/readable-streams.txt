//Readable Streams vs14
//      STREAMS - three types
// Writable streams - allow node js to write datea to a stream
// Readable streams - allow node.js to read data from a stream
// Duplex - can read and write to a stream

